name: 左侧栏—缩略图视图_suite
app: deepin-reader
description: ''
module: 左侧栏—缩略图视图
tags: []
fast_fail: false
env_check: []
setup:
- action: session_start
  command: deepin-reader ${TEST_FILES_DIR}/normal.pdf
  wait: 3.0
suites:
- id: suite_sidebar_toggle_s1
  name: 左侧栏 — 缩略图面板切换
  description: ''
  tags: []
  steps:
  - action: mouse_click
    selector:
      name: Button_ThumbnailToggle
      role: check box
  assert_steps:
  - action: assert_element
    selector:
      name: Form_DMainWindow
      role: form
teardown:
- action: session_stop
